class Solution:
    def minElement(self, nums: List[int]) -> int:
        mini=float('inf')
        for i in range(len(nums)):
            num=nums[i]
            sum=0
            while num>0:
                sum+=num%10
                num=num//10
            mini=min(sum,mini)
        return mini
            
